Index: src/widget/SplitContainer.js
===================================================================
--- src/widget/SplitContainer.js        (revision 8588)
+++ src/widget/SplitContainer.js        (working copy)
@@ -242,7 +242,16 @@
                        this.children[i].position = pos;
                        pos += size;
                }
+
+               this.onResize();
        },
+
+
+       /**
+        * Resize handler for to listen to the resizing
+        */
+       onResize: function(){
+       },
 
        _movePanel: function(/*Widget*/ panel, pos, size){
                if (this.isHorizontal){