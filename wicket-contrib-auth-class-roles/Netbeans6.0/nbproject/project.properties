build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/wicket-class-auth-roles.jar
dist.javadoc.dir=${dist.dir}/javadoc
file.reference.main-java=../../../wicket-auth-roles/src/main/java
file.reference.src-java=../src/java
file.reference.src-java-1=../src/java
file.reference.src-test=../src/test
file.reference.src-test-1=../src/test
file.reference.test-java=../../../wicket-auth-roles/src/test/java
jar.compress=false
javac.classpath=\
    ${libs.wicket.classpath}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
# Property libs.commons-logging.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.commons-logging.classpath=../../../../../../../Program Files/commons-logging/commons-logging.jar
# Property libs.log4j.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.log4j.classpath=../../../../../../../Program Files/log4j/dist/lib/log4j-1.2.13.jar
# Property libs.servlet.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.servlet.classpath=../../../../../../../Program Files/Tomcat 5.5/common/lib/servlet-api.jar
# Property libs.wicket.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.wicket.classpath=../wicket/wicket/target/wicket-1.2-SNAPSHOT.jar
main.class=
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}:\
    ${libs.commons-logging.classpath}:\
    ${libs.log4j.classpath}:\
    ${libs.servlet.classpath}
src.java.dir=${file.reference.src-java-1}
test.test.dir=${file.reference.src-test-1}
