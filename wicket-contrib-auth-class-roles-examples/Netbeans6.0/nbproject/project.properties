build.classes.dir=${build.web.dir}/WEB-INF/classes
build.classes.excludes=**/*.java,**/*.form
build.dir=build
build.ear.classes.dir=${build.ear.web.dir}/WEB-INF/classes
build.ear.web.dir=${build.dir}/ear-module
build.generated.dir=${build.dir}/generated
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
build.web.dir=${build.dir}/web
build.web.excludes=${build.classes.excludes}
client.urlPart=
compile.jsps=false
debug.classpath=${javac.classpath}:${build.classes.dir}:${build.ear.classes.dir}
debug.test.classpath=\
    ${run.test.classpath}
display.browser=true
dist.dir=dist
dist.ear.war=${dist.dir}/${war.ear.name}
dist.javadoc.dir=${dist.dir}/javadoc
dist.war=${dist.dir}/${war.name}
file.reference.main-java=../../../wicket-auth-roles-examples/src/main/java
file.reference.My_Documents-wicket-auth-roles-examples=../../../wicket-auth-roles-examples
file.reference.src-java=../src/java
file.reference.src-java-1=../src/java
j2ee.platform=1.4
j2ee.server.type=Tomcat55
jar.compress=false
javac.classpath=\
    ${libs.wicket.classpath}:\
    ${reference.wicket-class-auth-roles.jar}
# Space-separated list of extra javac options
javac.compilerargs=-Xlint:unchecked
javac.debug=true
javac.deprecation=false
javac.source=${default.javac.source}
javac.target=${default.javac.target}
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.preview=true
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
jspcompilation.classpath=${jspc.classpath}:${javac.classpath}
# Property libs.wicket.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.wicket.classpath=../../wicket/wicket/target/wicket-1.2-SNAPSHOT.jar
platform.active=default_platform
project.wicket-class-auth-roles=../../wicket-contrib-auth-class-roles/Netbeans6.0
reference.wicket-class-auth-roles.jar=${project.wicket-class-auth-roles}/dist/wicket-class-auth-roles.jar
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
# Space-separated list of JVM arguments used when running class with main method
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value):
runmain.jvmargs=
source.root=${file.reference.My_Documents-wicket-auth-roles-examples}
src.java.dir=${file.reference.src-java-1}
test.src.dir=
war.content.additional=
war.ear.name=wicket-class-auth-roles-examples.war
war.name=wicket-class-auth-roles-examples.war
war.package=true
web.docbase.dir=../src/webapp
